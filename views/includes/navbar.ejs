<!-- views/includes/navbar.ejs -->
 <style>
  .btn-search {
    padding:0 20px;
    background-color: #fe424d;
    color: white;
   border-radius: 40px;
  }
  .btn-search:hover{
    background-color: #fe424d;
    color: white;
  }
  .search-inp{
    font-size: 0.9rem;
    border-radius: 40px;
  }
  /* new */
  .navbar-collapse {
    background-color: white; /* Default background color */
    transition: background-color 0.3s ease;
  }

  /* .navbar-collapse.show {
    background-color: rgba(0, 0, 0, 0.8); /* Add background when expanded */
  /* } */

  .navbar-nav .nav-link {
    color: white; /* Adjust text color for visibility */
  }

 </style>

<nav class="navbar navbar-expand-md bg-body-white border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <div class="navbar-search ms-auto">
        <!-- <form class="d-flex" role="search">
          <input class="form-control search-inp me-2" type="search" placeholder="Search destinations" aria-label="Search">
          <button class="btn btn-search" type="submit">Search</button>
        </form> -->
        <form class="d-flex" id="searchForm">
          <input
            class="form-control search-inp me-2"
            type="search"
            name="query"
            id="searchInput"
            placeholder="Search destinations"
            aria-label="Search"
          />
          <button class="btn btn-search" type="submit">Search</button>
        </form>
        
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if (!currentUser) { %>
          <b><a class="nav-link" href="/signup">Sign up</a></b>
          <b><a class="nav-link" href="/login">Login</a></b>
        <% } else { %>
          <b><a class="nav-link" href="/logout">Log out</a></b>
        <% } %>
      </div>
    </div>
  </div>
</nav>



<!-- <nav class="navbar navbar-expand-md bg-body-white border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup ms-auto">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>


      <div class="navbar-search ms-auto">
      <form class="d-flex" role="search">
        <input class="form-control search-inp me-2" type="search" placeholder="Search destinations" aria-label="Search">
        <button class="btn btn-search" type="submit">Search</button>
      </form>
      </div>


      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home </a>
        <% if (!currentUser) {%>
        <b><a class="nav-link" href="/signup">Sign up</a></b>
        <b><a class="nav-link" href="/login">Login</a></b>
        <%} else {%> 
        <b><a class="nav-link" href="/logout">Log out</a></b>
        <%}%>
      </div>
    </div>
  </div>
</nav> -->
<script>
  document.getElementById('searchForm').addEventListener('submit', async function (event) {
    event.preventDefault(); // Prevent the form from reloading the page

    const query = document.getElementById('searchInput').value; // Get the search input value
    const response = await fetch(`/search?query=${query}`);
    const listings = await response.json(); // Parse the JSON response

    const listingsContainer = document.getElementById('listingsContainer'); // The container for listings
    listingsContainer.innerHTML = ''; // Clear existing listings

    // Render new listings
    if (listings.length > 0) {
      listings.forEach(listing => {
        const listingCard = `
          <div class="col-lg-3 col-md-4 col-sm-6 col-12">
            <div class="card">
              <a href="/listings/${listing._id}">
                <img src="${listing.image.url}" class="card-img-top" alt="${listing.title}">
                <div class="card-body">
                  <h5 class="card-title">${listing.title}</h5>
                  <p class="card-text">₹${listing.price.toLocaleString('en-IN')} / Night</p>
                </div>
              </a>
            </div>
          </div>
        `;
        listingsContainer.innerHTML += listingCard;
      });
    } else {
      listingsContainer.innerHTML = '<p>No results found.</p>';
    }
  });
</script>
