<!-- views/includes/navbar.ejs -->
 <style>
  .btn-search {
    padding:0 20px;
    background-color: #fe424d;
    color: white;
   border-radius: 40px;
  }
  .btn-search:hover{
    background-color: #fe424d;
    color: white;
  }
  .search-inp{
    font-size: 0.9rem;
    border-radius: 40px;
  }
  /* new */
  .navbar-collapse {
    background-color: white; /* Default background color */
    transition: background-color 0.3s ease;
  }

  /* .navbar-collapse.show {
    background-color: rgba(0, 0, 0, 0.8); /* Add background when expanded */
  /* } */ 

  .navbar-nav .nav-link {
    color: white; /* Adjust text color for visibility */
  }

 </style>
<nav class="navbar navbar-expand-md bg-body-white border-bottom sticky-top">
  <div class="container-fluid">
    <!-- Brand Logo and Text -->
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="fa-regular fa-compass me-2"></i>
      <span class="d-none d-md-inline">Pulkit's Homestays</span> <!-- Visible on medium+ screens -->
    </a>

    <!-- Toggler Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Navbar -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link d-md-none" href="/listings">Pulkit's Homestays</a> <!-- Visible on small screens -->
      </div>

      <!-- Right-Aligned Links -->
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if (!currentUser) { %>
          <b><a class="nav-link" href="/signup">Sign up</a></b>
          <b><a class="nav-link" href="/login">Login</a></b>
        <% } else { %>
          <b><a class="nav-link" href="/logout">Log out</a></b>
        <% } %>
      </div>
    </div>
  </div>
</nav>



